<html>
  <body>
  <div class="card-body">
    <h1>Voici le contenu du potin n°<%=params['index']%> : </h1>
    <p>
      <%= Gossip.find(params['index']).content%></p>

    <h2>Et plus d'infos sur l'auteur du potin: </h2>
    <p><strong>Son prénom :</strong> <%= User.find(params['index']).first_name%></p>
    <p><strong>Son nom : </strong><%= User.find(params['index']).last_name%></p>
    <p><strong>Son âge :</strong> <%= User.find(params['index']).age%></p>

    <a href='/'>Lien pour revenir à la page d'accueil</a>
  </div>

  </body>
</html>
